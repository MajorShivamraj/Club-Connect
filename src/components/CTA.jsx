// src/components/CTA.js

import React from 'react';

const CTA = () => {
  return (
    <section id="signup" className="section cta-section">
      <div className="container text-center">
        <h2>Join ClubConnect and never miss a campus event again!</h2>
        <button className="btn btn-light-large">Sign Up Now</button>
      </div>
    </section>
  );
};

export default CTA;